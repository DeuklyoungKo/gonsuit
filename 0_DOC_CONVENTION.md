# 프로젝트 문서 관리 규약 (Doc Convention)

> **이 문서의 용도**: 다른 프로젝트·다른 컴퓨터에서도 동일한 방식으로 개발 문서를 관리하기 위한 **이식 가능한 규약**이다.
> 새 프로젝트를 시작하거나 기존 프로젝트를 정리할 때, AI에게 **"`0_DOC_CONVENTION.md`를 읽고 이 방식으로 문서를 정리해줘"** 라고 지시하면 된다.
>
> 이 규약은 고앤슈트(gonsuit) 프로젝트에서 실제로 검증되었다. 레퍼런스 구현은 같은 저장소의 `CLAUDE.md`를 참조.
> **이 파일은 각 프로젝트 저장소 루트에 복사해서 쓴다.** (컴퓨터별 환경 설정은 동기화하지 않는다 — 머신마다 다르기 때문)

---

## 0. 왜 이 규약이 필요한가

**목적은 하나다 — 작업 컴퓨터를 옮겨도 AI가 "지금 어디까지 됐고 다음에 뭘 해야 하는지"를 즉시 파악하게 하는 것.**

그래서 두 가지가 강제된다.

1. **모든 기준 문서는 git으로 추적되는 저장소 파일이어야 한다.** 로컬 AI 메모리(`~/.claude/projects/*/memory/`)나 개인 메모는 해당 PC에만 존재하므로 크로스 머신 기준이 될 수 없다.
2. **한 사실은 한 곳에만 적는다 (SSOT).** 같은 사실이 두 문서에 있으면 반드시 한쪽이 먼저 썩는다.

> 실제 사례: 고앤슈트에서 `/resources` 페이지를 삭제했으나 `CLAUDE.md`는 3개월간 "존재함"으로 기재. 그동안 기능 커밋 10건, 문서 커밋 0건. 다른 컴퓨터의 AI는 없는 페이지를 있다고 믿고 작업했다.

---

## 1. 문서 구성

| 문서 | 역할 | 소유하는 정보 | 성격 |
| :--- | :--- | :--- | :--- |
| `1_PRD.md` | 제품 요구사항 정의서 | 제품이 **왜/무엇** — 정체성, 포지셔닝, 요구사항, 수익 방향 | 방향성(durable) |
| `2_PDP.md` | 개발 계획서 | 해당 단계 실행 계획. **완료 시 상단에 완료 표기 후 동결** | 계획 → 과거 기록 |
| `3_DesignGuide.md` | 디자인 가이드 | 색상표·컴포넌트 스펙·타이포그래피·접근성 | 사양(durable) |
| `4_LRP.md` | 장기 로드맵 | 장기 전략·비전, 확장 아이디어 | 비전 |
| **`CLAUDE.md`** | **운영 SSOT + 허브** (AI가 매 세션 자동 로드) | 프로젝트 분류, **현재 작업 상태**, 기술 스택, 구조, 컨벤션 = **현재값** | 현재형·실행지향 |
| `dev.md` | 업무에 필요한 명령어 요약 | 자주 쓰는 실행·빌드·배포 명령어 | 👤 **사람이 관리** |

**선택 문서**
- `3_DesignGuide.md` — UI가 있는 프로젝트에만. 없으면 번호를 비우고 `1·2·4`만 둔다.
- `dev.md` — **사람이 직접 관리하는 개인 치트시트.** AI는 참조만 하고 임의로 수정하지 않으며, 없다고 새로 만들지도 않는다.

### 이 구성이 나온 배경
원래 `dev.md` 하나에 개발 내용을 전부 모았다가 → `1_PRD`·`2_PDP`·`3_DesignGuide`·`4_LRP`로 분화했다. 그런데 **이 넷은 모두 잘 안 바뀌는(durable) 문서라 "지금 어디까지 했나"를 담을 곳이 없었다.** 그 공백을 `CLAUDE.md`가 메운다.

---

## 2. 핵심 규칙 6가지

### ① 현재값은 `CLAUDE.md`만 소유한다
스택·구조·상태·규칙은 `CLAUDE.md`에만 적는다. 다른 문서는 반복하지 말고 **"현재 구현은 `CLAUDE.md` 참조"로 링크**한다.

**판별 기준 — "이 문장이 6개월 뒤에도 그대로 참인가?"**

참이면 durable 문서(`1_PRD`·`3_DesignGuide`·`4_LRP`), 틀려질 수 있으면 `CLAUDE.md`다.

| 문장의 종류 | 예시 | 6개월 뒤 | 소유 문서 |
| :--- | :--- | :--- | :--- |
| 정체성·목적 | "이 제품은 OO를 위한 도구다" | ✅ 참 | `1_PRD.md` |
| 전략·방향 | "장기적으로 B2B로 확장한다" | ✅ 참 | `4_LRP.md` |
| 계획·실적 기록 | "1차 릴리스는 X를 범위로 한다/했다" | ✅ 참 | `2_PDP.md` |
| 디자인 사양 | "주요 색상은 #RRGGBB, 본문 16px" | ✅ 참 | `3_DesignGuide.md` |
| **현재 상태** | "현재 버전 v1.2, **운영중**" | ❌ 바뀜 | **`CLAUDE.md`** |
| **현재 구현** | "라우트는 `/a`·`/b` 2개" | ❌ 바뀜 | **`CLAUDE.md`** |
| **다음 할 일** | "다음은 결제 연동" | ❌ 바뀜 | **`CLAUDE.md`** |

> ⚠️ 자주 틀리는 지점: **같은 대상을 말해도 문장의 종류에 따라 소유 문서가 갈린다.**
> "제품 A는 시니어 대상 접근성 도구다" → durable / "제품 A는 **출시 준비중**이다" → `CLAUDE.md`.
> durable 문서에 상태 단어(`운영중`·`준비중`·`v1.2`)가 보이면 **그 단어만 도려내고** 나머지 문장은 남긴다.

### ② `CLAUDE.md` 최상단에 "현재 작업 상태" 섹션을 둔다
아래 템플릿을 그대로 쓴다.

```markdown
## 현재 작업 상태 ⚠️ 세션 시작 시 먼저 확인

> **최종 갱신: YYYY-MM-DD**
> 이 섹션은 누적 로그가 아니라 스냅샷이다. 항상 덮어쓰며, 지나간 이력은 남기지 않는다. (이력은 `git log` 참조)

**진행 중 작업**: (없음 / 작업명)

**다음 할 일**
1. ...
2. ...

**최근 결정 사항**
- ...
```

### ③ "현재 작업 상태"는 스냅샷이다 — 이력을 쌓지 않는다
완료 항목은 지우고 덮어쓴다. 이력은 이미 `git log`에 있다.
**여기에 로그를 누적하면** 매 세션 컨텍스트를 낭비하고, 규칙과 과거 사실이 섞여 AI가 오래된 정보를 현재 규칙으로 오인한다. (`dev.md`가 비대해졌던 것과 같은 실패)

### ④ 갱신 트리거 — 커밋과 같은 묶음으로
**작업 완료 후 커밋할 때, 변경된 현재값과 "현재 작업 상태"를 같은 커밋에 포함한다.**
"나중에 정리"는 반드시 누락된다. 이 규칙이 없으면 위 실제 사례(기능 10건 / 문서 0건)가 그대로 재현된다.

### ⑤ 경로는 저장소 루트 기준 상대경로로만
`D:\...`, `E:\...` 같은 **절대경로 금지** — 컴퓨터마다 드라이브·폴더명이 달라 그대로 깨진다.

- 저장소 내부: `src/app/page.tsx`
- 저장소 외부(형제 프로젝트): `../<프로젝트폴더>/` + **"해당 PC에 없을 수 있음"** 주석 필수
- 외부 저장소를 가리킬 때는 상대경로 대신 **저장소 URL**을 쓰는 편이 안전하다 (폴더 구조가 PC마다 다르므로)

### ⑥ 완료된 계획 문서는 동결한다
살아있는 척 두지 말고 상단에 완료 표기 후 아카이브. 이후 작업은 `CLAUDE.md`가 관리한다.

```markdown
> ## ✅ 1차 런칭 완료 — 아카이브(동결) 문서
> 이 문서는 초기 계획의 완료 기록이다. 이후 진행 상황은 `CLAUDE.md`가 관리한다.
> 새 작업 계획을 여기에 추가하지 않는다.
```

---

## 3. Notion ↔ GitHub 역할 분담

기획은 Notion, 실행은 GitHub으로 나뉜다. **경계를 지키지 않으면 같은 사실이 양쪽에 생겨 한쪽이 썩는다.**

| | Notion | GitHub 저장소 |
| :--- | :--- | :--- |
| **소유하는 것** | 기획·아이디어·검토 기록, 계정·비용·인프라 운영 | **현재값 전부** — 스택, 구조, 상태, 다음 할 일 |
| **답하는 질문** | 왜 이걸 하려 했나 | 지금 어떻게 되어 있나 |
| **성격** | 착수 전·주변부 | 착수 후·본체 |

**규칙**
- **Notion에 현재 상태를 적지 않는다.** 진행률·완료 여부·기술 스택·디렉토리 구조는 `CLAUDE.md`가 소유한다. Notion 대시보드에 "현재 진행" 같은 섹션을 두면 반드시 낡는다.
- Notion 프로젝트 페이지는 상세를 복사하지 말고 **GitHub 저장소를 링크로 가리킨다.**
- 프로젝트가 착수되면 무게중심이 GitHub으로 넘어간다. Notion에는 **착수 전 기획 기록만 남긴다.**
- 반대로 **GitHub에 계정·비용·구독 정보를 적지 않는다.** 그건 Notion 운영 영역이다.

> AI가 매 세션 자동으로 읽는 것은 **`CLAUDE.md`뿐이다.** Notion은 요청받아야 열어본다.
> 따라서 **AI가 지켜야 할 규칙은 Notion이 아니라 `CLAUDE.md`·이 문서에 적는다.** Notion에만 있는 규칙은 정작 필요한 순간에 읽히지 않는다.

---

## 4. 신규 프로젝트 적용 절차

```
1. 1_PRD.md / 2_PDP.md (/ 3_DesignGuide.md) / 4_LRP.md 작성
2. CLAUDE.md 생성 — 아래 뼈대 순서를 따른다
3. 각 durable 문서 상단에 SSOT 경계 선언 추가
4. 첫 커밋
```

**`CLAUDE.md` 뼈대**
```
프로젝트 개요        ← 분류(Official/Private) 명시
현재 작업 상태 ⚠️    ← §2-② 템플릿
기술 스택
디렉토리 구조         ← 상대경로
[프로젝트별 도메인 섹션]
코딩 컨벤션
배포 프로세스
Git 커밋 규칙
문서 관리 체계        ← 이 문서 참조 링크
```

**durable 문서 상단에 넣을 SSOT 경계 선언 예시**
```markdown
> **이 문서의 범위**: 제품이 왜/무엇인가만 다룬다.
> 기술 스택·구조·현재 상태는 `CLAUDE.md`가 단일 진실원(SSOT)이다. 중복 기술 금지.
```

---

## 5. 기존 프로젝트 이관 절차 (드리프트 감사)

이미 문서가 있는 프로젝트에 적용할 때. **고앤슈트에서 이 절차로 실제 드리프트 6건을 검출했다.**

```
1. 실제 코드 스캔          → 라우트·파일·설정 목록 추출
2. 문서와 대조             → 유령 항목(문서에만 있음) / 누락 항목(코드에만 있음)
3. 절대경로 스캔           → 상대경로로 전환
4. 문서에 적힌 경로 실존 확인 → 전수 검사
5. 현재값 중복 검사         → 상태·스택이 여러 문서에 흩어졌는지
6. CLAUDE.md 재편          → "현재 작업 상태" 신설, SSOT 경계 선언
7. 완료된 계획 문서 동결
```

**검출된 실제 사례**: 삭제된 페이지가 문서에 잔존 / 신규 페이지 3개 누락 / 잘못된 패키지 ID / 존재하지 않는 절대경로 3건.

---

## 6. 안티패턴 — 하지 말 것

| ❌ | 이유 |
| :--- | :--- |
| `CLAUDE.md`에 진행 **이력**을 누적 | 컨텍스트 낭비 + 규칙과 과거 사실이 섞임. 이력은 `git log` |
| 같은 사실을 두 문서에 기재 | 반드시 한쪽이 썩는다 |
| 문서에 절대경로 기재 | 다른 컴퓨터에서 깨짐 |
| 완료된 계획 문서를 방치 | 미완료 `[ ]`가 현재 할 일로 오인됨 |
| durable 문서에 현재 상태 기재 | 상태 변경 시 갱신 누락 |
| 갱신을 "나중에" | 누락된다. 커밋과 같은 묶음으로 |
| AI가 `dev.md`를 임의 수정 | 사람 소유 문서. 제안만 한다 |
| 컴퓨터별 환경 설정을 동기화 | 머신마다 환경이 달라 충돌. **규약만 공유한다** |
| Notion에 진행 상황·현재 상태 기재 | 자동 로드되지 않아 갱신을 놓친다. 현재값은 `CLAUDE.md` (§3) |
| AI 규칙을 Notion에만 기재 | 매 세션 읽히는 건 `CLAUDE.md`뿐. 필요한 순간에 안 읽힌다 |
